{
  "name": "understrap-child",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "esbuild assets/ts/pokemon.ts --bundle --outfile=assets/js/pokemon.js --platform=browser --sourcemap",
    "watch": "npm run build -- --watch"
  }
}
